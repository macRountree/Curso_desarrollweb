//WHILE

let i = 0; //Inicializamos el while

while (i < 15) {
  if (i % 2 === 0) {
    console.log(`numero ${i} es par`);
  } else {
    console.log(`El numero ${i} no es par`);
  }

  i++; //incremento
}

//el while se ejecuta con una condicion verdaderS
