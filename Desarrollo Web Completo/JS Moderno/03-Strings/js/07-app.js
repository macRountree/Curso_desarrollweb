const producto = "monitor";

//convertir en mayusculas  Requerimos que los textos esten en mayuscu
//si tenemos un millon deregistros podemos
console.log(producto.toUpperCase());

//convertir en minusculas
console.log(producto.toLowerCase());

//antes de guardar correos a una BD se convierten en minusculas y utilizamos lowerCase

const precio = 300;

console.log(precio.toString());
