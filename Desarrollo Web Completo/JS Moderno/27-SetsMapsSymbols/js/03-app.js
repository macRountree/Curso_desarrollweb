// Los maps son listas ordenadas con llave valor .. pueden ser cualquier tipo de dato

// Los maps tienen mejor performance que un objeto..

const cliente = new Map();

// Para agregar eleementos utilizamos set
cliente.set('nombre', 'karen');
cliente.set('tipo', 'standart');
cliente.set('saldo', 200);

console.log(cliente);
