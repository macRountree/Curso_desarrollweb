//?DIFERENCIAS ENTRE METODO Y FUNCION

const num1= 20;
const num2= '20';

console.log(parseInt(num2)); //Esto es una funcion solo tiene un parentisis y dentro colocamos la variable

console.log(num1.toString()); //esto es un metodo tienen un punto seguido de la variable
