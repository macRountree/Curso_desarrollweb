const tech = ['vue', 'js', 'html'];
const numeros = [10, 20, 30];

//in

let result = tech.includes('vue');

result = tech.findIndex(techno => techno === 'js');

//*some devuelve true si un elemento cumple con una condicion

console.log(result);
